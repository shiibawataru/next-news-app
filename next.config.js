/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  nextConfig,
  // images: {
  //   domains: ["assets.media.com", "assets.media-platform.com"],
  // },
};
